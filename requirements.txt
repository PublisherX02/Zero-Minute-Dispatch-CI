# ── API backend ────────────────────────────────────
fastapi==0.111.0
uvicorn==0.30.0
pydantic==2.7.0
google-generativeai==0.7.0
python-dotenv==1.0.0
requests

# ── NLP / ML ───────────────────────────────────────
# Use CPU torch wheel for Docker (saves ~1 GB vs CUDA wheel)
# Install torch separately: pip install torch --index-url https://download.pytorch.org/whl/cpu
transformers==4.41.0
accelerate
sentencepiece

# ── Streamlit frontend ─────────────────────────────
streamlit==1.35.0
streamlit-folium
folium
branca

# ── Testing ────────────────────────────────────────
pytest==8.2.0